<div class="container">
  <%= render 'layouts/banner', title: @recipe.name, subtitle: "Discover more below", image_url: @recipe.image_url %>

  <div class="container-fluid d-flex justify-content-between align-items-center mb-4">
    <h2 class="mb-0">Recipe</h2>
    <%= link_to 'Back', category_path(@category), class: "btn btn-info" %>
  </div>

  <%= render 'recipe', description:  @recipe.description %>
</div>
